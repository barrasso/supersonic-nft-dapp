/* MAIN ROUTER FILE (routes.js) */

// Include the config and db variables
const config = require('../../config/config.js');
const db = require('../../config/db.js');

// Include node module libraries
const request = require("request");

// Include models
var Soundbite = require('./models/soundbite.js');

// Define routes
module.exports = function(app) {

};